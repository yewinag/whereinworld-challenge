import React from 'react';
import Button from '../Button';

function Header() {
  return (
    <header>
      <h2>Where in the world?</h2>
      <Button />
    </header>
  );
}

export default Header;
